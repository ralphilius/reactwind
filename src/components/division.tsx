import { DivisionProps } from "./types";

export const Division = (props: DivisionProps) => {
  return (
    <div {...props}></div>
  )
}

<Division p="px" w="full" justify="around" justifyItems="center" items="center" placeContent="center"/>